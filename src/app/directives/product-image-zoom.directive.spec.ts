import { ProductImageZoomDirective } from './product-image-zoom.directive';

describe('ProductImageZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductImageZoomDirective();
    expect(directive).toBeTruthy();
  });
});
